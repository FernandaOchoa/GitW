# Hola desde un readme :D

## Instrucciones
Para crear un repositorio necesitas:
En la carpeta a versionar:
* git init 
* git add <name file> o <.>
* git commit -m "mensaje"
* git remote add origin "<url>"
* git push origin master
